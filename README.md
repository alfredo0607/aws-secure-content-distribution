# aws-secure-content-distribution
Implements a secure private content delivery architecture on AWS using CloudFront, S3 private origins, Origin Access Control (OAC), SigV4 request signing, and expiring Signed URLs managed by the backend.
